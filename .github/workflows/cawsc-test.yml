name: Test Configure AWS Credential
on:
  push:
    branches:
      - main  # Triggers the pipeline when code is pushed to the main branch
  pull_request:
    branches:
      - main  # Runs Terraform Plan for pull requests
  workflow_dispatch:


permissions:
  id-token: write
  contents: read

jobs:
  cawsc:
    runs-on: ubuntu-latest

    steps:
      - name: CAWSC
        uses: aws-actions/configure-aws-credentials@main
        with:
          aws-region: us-east-1
          role-to-assume: arn:aws:iam::945252183366:role/cumulus-s3-bucket-creation-oidc-role
          role-duration-seconds: 900
          role-session-name: TestCAWSC
      - name: Whoami
        run: |
          aws sts get-caller-identity
